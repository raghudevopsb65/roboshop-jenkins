- name: template
  template:
    src: pipeline.xml
    dest: /tmp/pipeline.xml
  loop:
    - { repoName: "cart", componentName: "cart" }
- name: Create a jenkins job using the token
  community.general.jenkins_job:
    config: "{{ lookup('file', '/tmp/pipeline.xml') }}"
    name: "test"
    url: "http://jenkins.devopsb65.online:8080"
    user: "admin"
    token: "11f73e73eb52befc32e4d0aed578a10633"


